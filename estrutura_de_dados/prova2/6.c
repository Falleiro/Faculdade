#include <stdio.h>
#include <stdlib.h>

int main()
{
    int qtd, x;
    printf("Digite a quantidade de numeros que deseja inserir:");
    scanf("%d",&qtd);
    for (int i = 0; i < qtd; i++)
    {
        printf("Digite o numero que deseja inserir:");
        scanf("%d",x);
        //incluir
    }
    

    return 0;
}